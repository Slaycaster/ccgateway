{
  "name": "ccgateway",
  "version": "0.1.0",
  "description": "Multi-agent orchestration layer built on Claude Code CLI",
  "type": "module",
  "bin": {
    "ccg": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "tsx": "^4.19.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^22.14.1",
    "vitest": "^3.1.1"
  }
}
